services:
  app:
    build:
      context: ..
      dockerfile: ms-notification/Dockerfile
    container_name: weather_forecast_sub_notification_app
    ports:
      - "${APP_PORT}:${APP_PORT}"
    volumes:
      - .env.dev:/app/ms-notification/.env.dev
      - ./logs:/app/ms-notification/logs
